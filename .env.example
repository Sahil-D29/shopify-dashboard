# ===========================================
# Shopify Dashboard - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in the values:
#   cp .env.example .env.local
#
# Generate secrets with: node scripts/generate-secrets.js

# --- Database (Neon PostgreSQL) ---
DATABASE_URL=postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/db?sslmode=require

# --- Auth (NextAuth / Auth.js) ---
# IMPORTANT: NEXTAUTH_URL must match the URL your app runs on.
# Local:      http://localhost:3002
# Production: https://your-app.onrender.com
# This URL is used to construct the Google OAuth callback URI:
#   {NEXTAUTH_URL}/api/auth/callback/google
# That EXACT URI must also be added to Google Cloud Console → Authorized redirect URIs.
NEXTAUTH_URL=http://localhost:3002
NEXTAUTH_SECRET=       # Run: node scripts/generate-secrets.js
ENCRYPTION_KEY=        # 64 hex chars. Run: node scripts/generate-secrets.js

# --- Google OAuth (optional — enables "Continue with Google") ---
# Get from: Google Cloud Console → APIs & Services → Credentials
# IMPORTANT: Add redirect URI in Google Console:
#   Local:      http://localhost:3002/api/auth/callback/google
#   Production: https://your-app.onrender.com/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# --- Shopify ---
SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=

# See docs/ENV_VARIABLES.md for the full reference.
